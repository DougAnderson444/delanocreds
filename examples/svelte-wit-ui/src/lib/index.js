// place files you want to import through the `$lib` alias in this folder.
import { default as UIOnly } from './ui/UIOnly.svelte';
import { default as Full } from './full/FullWallet.svelte';

export const examples = [
	// Full,
	UIOnly
];
